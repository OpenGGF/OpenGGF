# Sample Flappy mod source

`project/` is the checked-in output of `ggfmod init --id sample-flappy --package
example.flappysample`, hand-adapted into the gallery's sixth sample. The integration
test copies this source fixture and exercises its Maven art/level conversion
lifecycle, Java compilation, and validated deterministic packaging. No built jar is
checked in.
