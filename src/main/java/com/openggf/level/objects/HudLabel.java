package com.openggf.level.objects;

/** Closed set of stock label-art identities available to a HUD profile. */
@com.openggf.game.ModApi
public enum HudLabel {
    SCORE,
    TIME,
    RINGS,
    LIVES
}
