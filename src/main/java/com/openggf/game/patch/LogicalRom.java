package com.openggf.game.patch;

/**
 * Logical ROM identity a patch can require independently of the physical
 * image that supplies its bytes.
 */
public enum LogicalRom {
    /** Sonic & Knuckles, from a standalone image or the S&K window of S3K. */
    SK
}
