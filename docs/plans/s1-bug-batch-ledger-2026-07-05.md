# S1 Bug Batch Triage Ledger (2026-07-05)

Task 1 output for `docs/superpowers/plans/2026-07-05-s1-bug-triage-and-fix-plan.md`. One row
per bug named in that plan's Workstream Summary table (25 bugs across 8 workstreams). All
symptom bugs below are **untriaged** as of this ledger — Task 1 only established the baseline
and reproduction scaffolding; no root-cause or fix work has happened yet.

## Baseline result summary (this session)

- **Step 2 trace baseline** (`TestS1Ghz1TraceReplay`, `TestS1Mz3CompleteRunTraceReplay`,
  `TestS1Syz1CompleteRunTraceReplay`, `TestS1Syz3CompleteRunTraceReplay`,
  `TestS1Lz1CompleteRunTraceReplay`, `TestS1Lz2CompleteRunTraceReplay`,
  `TestS1Slz2CompleteRunTraceReplay`, `TestS1Slz3CompleteRunTraceReplay`): **8/8 GREEN**,
  0 errors, matching `docs/TRACE_FRONTIER_LOG.md`'s "full S1 sweep remains 29/29 green" note.
  - First attempt with the repo-default `forkCount=4` produced 6 spurious
    `UnsatisfiedLinkError: Failed to locate library: lwjgl.dll` /
    `NoClassDefFoundError: GlfwKeyNameResolver$Holder` errors — a parallel-fork native-library
    extraction race in this worktree, not a ROM/trace regression. Re-run with
    `-Dsurefire.forkCount=1` gave a clean `MSE:OK modules=1 passed=8 failed=0 errors=0 skipped=0`.
    Anyone re-running the Step 2 command verbatim in a fresh worktree may hit the same
    fork-race flake; pass `-Dsurefire.forkCount=1` if so.
- **Step 3 focused non-trace suite** (16 classes covering special stage input, object
  visuals, badniks, springs, staircases, boss/rewind graphs, audio/water rewind):
  **69/69 tests GREEN**, 0 failures/errors, across all 16 named classes (all classes exist;
  no `-DfailIfNoTests=false` needed since none were missing). Per-class counts recorded in
  `target/surefire-reports/*.txt` in the worktree.

**Net effect:** none of today's currently-tracked bugs are caught by the existing automated
suite. Every row below needs either a new targeted test/BizHawk probe to reproduce the
symptom, or an existing test's assertions widened to cover the described defect before any
fix can be verified.

## Ledger

| # | Workstream | Bug (symptom) | Zone / act / coordinates | Reproduction route | Suspected owner | Current automated coverage | First failing command | Status |
|---|---|---|---|---|---|---|---|---|
| 1 | Input and special stage | Can't jump in special stages | Special Stage (chaos emerald stage entered via checkpoint monitor, any zone) | Reproduced with a new `Sonic1SpecialStageManagerTest#testHeldJumpActionMakesGroundedSonicAirborneWithJumpForceVelocity`: driving `Sonic1SpecialStageManager.processJump()` with the Mega Drive A-button bit (`0x40`, exactly what `GameLoop.updateSpecialStageInput()` forwards for a jump bound to the A action) failed to make Sonic airborne. Root cause: `e2472501f` ("feat: route gameplay through logical input") replaced GameLoop's old `pressedButtons \|= 0x70` (all of A/B/C, matching ROM `SonicSS_Jump`'s `andi.b #btnABC,d0`) with `InputActionMasks.toMegaDriveButtonBits(p1.actionPressedMask())`, which only sets the bit(s) for whichever action button was actually pressed — but `Sonic1SpecialStageManager.processJump()` still checked `pressedButtons & AbstractPlayableSprite.INPUT_JUMP` (`0x10`), which only coincidentally aliases the Mega Drive B-button bit. Jump only worked when B specifically was held; A (the common default jump bind) and C did nothing. Fixed by checking the new `Sonic1Constants.SS_JUMP_BUTTONS` (`0x70`, any of A/B/C) instead, matching the ROM check and the identical `& 0x70` pattern already used by `Sonic3kSpecialStagePlayer.updateJump()`. | `Sonic1SpecialStageManager` (fixed) | `Sonic1SpecialStageManagerTest` (12 tests, +1 new) GREEN after the fix; RED before it (`Jump X velocity should derive from SS_JUMP_FORCE` failed, and before an intermediate iteration, `Holding jump on the ground should make Sonic airborne` failed). | `mvn "-Dtest=Sonic1SpecialStageManagerTest" -Dsurefire.forkCount=1 test` (RED before fix, GREEN after) | fixed |
| 2 | Input and special stage | Held direction feels intermittently weak | General gameplay / special stage, input-timing dependent, no fixed coordinates | Investigated per Task 2's brief: added `TestInputHandlerLogicalSnapshot#holdingDirectionForOneHundredTwentyFramesNeverReadsZeroedWhileKeyboardKeyStaysDown` (keyboard-only), `#...WhileGamepadDpadStaysHeld` (gamepad-only via `FakeGamepadStateSource`), and `TestLiveRewindLogicalInput#heldDirectionNeverReplaysAsZeroedAcrossOneHundredTwentyAppendedLiveRewindFrames` (records 120 held-right frames through `LiveRewindInputSource.appendFrame()` and replays each via `RecordedInputSnapshots.fromBk2()`). All three pass immediately on the unmodified tree — no frame ever reads a zeroed `heldMask()` while the physical source stays held, in the keyboard path, the gamepad-poll path, or the live-rewind record/replay path. Also reviewed `LiveRewindManager`/`LiveRewindStepper`: rewind coasting after a key release causes `stepInternal()` to skip the normal gameplay update entirely for those frames (by design — the game is stepping backward, not reading live held input), but does not zero the live `InputHandler` snapshot itself. No reproducible zeroing found at the `InputHandler`/`LogicalInputSnapshot`/`GamepadInputManager`/`LiveRewindInputSource` layer; per the brief, no speculative fix was landed. | `InputHandler`, `LogicalInputSnapshot`, `GamepadInputManager`, `LiveRewindInputSource` (all investigated, none changed) | `TestInputHandlerLogicalSnapshot` (+2 new tests) and `TestLiveRewindLogicalInput` (+1 new test), all GREEN without any production change — the added coverage did not reproduce the symptom. | `mvn "-Dtest=TestInputHandlerLogicalSnapshot,TestLiveRewindLogicalInput" -Dsurefire.forkCount=1 test` (GREEN both before and after; no RED reproduction found) | blocked-needs-capture |
| 3 | S1 object visuals | Egg prison button persists | End-of-act boss arena (Act 3 of any zone with an Egg Prison capsule, e.g. GHZ3) | Reproduced with new `TestSonic1EggPrisonButtonGraphRewind#s1EggPrisonButtonGoesBlankAfterExplosionPhaseAndStaysBlank`: triggers `onSolidContact`, drives the capsule through its 60-frame explosion phase, and asserts the button's mapping frame. Root cause: `docs/s1disasm/_incObj/3E Prison Capsule.asm` Pri_Switch/Pri_Explosion are the SAME ROM object slot — the switch and the explosion/animal-spawn driver are one object, so ROM freezes the switch's flash animation the frame it leaves Pri_Switch, then explicitly blanks it (`move.b #6,obFrame(a0)`, line 137) at `.makeanimal`. The engine modeled the button/body as two cooperating objects but never blanked the button, so it kept toggling its two flash frames and rendering forever. Fixed by adding `Sonic1EggPrisonButtonObjectInstance.goBlank()` (freezes on trigger, blanks at the parent's explosion→animal-spawn transition) called from `Sonic1EggPrisonObjectInstance.updateExploding()`. | `Sonic1EggPrisonButtonObjectInstance`, `Sonic1EggPrisonObjectInstance` (fixed) | `TestSonic1EggPrisonButtonGraphRewind` (+1 new test) GREEN after the fix. | `mvn "-Dtest=TestSonic1EggPrisonButtonGraphRewind" -Dsurefire.forkCount=1 test` (GREEN after fix) | fixed |
| 4 | S1 object visuals | Glass reflection oscillates incorrectly | Marble Zone (MZ), glass block pillar reflections (originally reported against LZ; the object is MZ's `Obj30`/`MZ Large Green Glass Blocks`) | Re-investigated after a user-reported real-gameplay observation (a type-4 switch-activated pillar sits stationary while its reflection floats up to 144px away in mid-air) appeared to contradict the first investigation's "already ROM-accurate" verdict. Byte-level re-verification against the assembled `docs/s1disasm/sonic.lst` (not just the `_incObj` transcription) confirmed the original branch-sense reading (`Glass_Type03`/`Glass_Type04`'s `btst #3,obSubtype(a0)` / `beq.s`) was correct and NOT the bug: the reflection's bit-3-set path genuinely falls straight into the `(v_oscillate+$12) - $10` oscillator formula unconditionally, ignoring `glass_dist`, exactly as the first investigation and the existing characterization test describe. The **actual** defect is a separate, previously-mischaracterized bug: `Sonic1GlassReflectionInstance.java` (short-variant sync, was line 137) read `parent.getBaseY()` (the block's static spawn-time baseline) instead of `parent.getY()` (the block's current, live Y). `Glass_Reflect34` (`sonic.lst` `BB5E: 3169 000C 0030`) is byte-verified to execute `move.w obY(a1),objoff_30(a0)` — source displacement `$0C` is `obY` (live position), not `$30` (`objoff_30`, the static baseline) — so the reflection must resync its Y-reference from the parent's CURRENT position every frame, not its spawn baseline. This single anchor bug fully explains both halves of the user's report: while untriggered, `glass_dist` sits at its ROM-mandated initial `$90` (144px) for the whole time the block stays raised, so the buggy static-baseline reflection floats 144px away from the block continuously (the "floating in mid-air" symptom); once triggered, the block's Y coincidentally approaches its own spawn baseline as `glass_dist` empties toward 0, making the previously-mispositioned reflection appear to "snap into" the glass and read as static. Fixed by anchoring `baseY = parent.getY()` instead of `parent.getBaseY()` in the short-variant (subtypes 3-4) sync; the oscillator formula itself (`applyReflectionMovement()` case 3/4) is untouched — it was already byte-accurate. A new test (`TestSonic1GlassReflectionGraphRewind#reflectionTracksParentsCurrentYThroughRealUpdatesIncludingAfterTriggeredLowering`) drives a real Type04 block + its spawned reflection child through actual `ObjectManager` update cycles, both before and after a real switch press lowers the block, and asserts `\|reflection.getY() - block.getY()\| <= 0x40` (the ROM shimmer swing) throughout; it was RED (delta=160px) against unmodified code and GREEN after the fix. The existing `type4ReflectionOscillatesIndependentlyOfParentGlassDistPerRom` characterization test's independence claim (reflection Y does not depend on `glass_dist`) remains correct and was kept, with its formula assertion's anchor updated from the parent's raw spawn Y to the parent's actual constructed Y (since that test never calls `parent.update()`, so `parent.getY()` differs from `spawn.y()` by the constructor's initial `glass_dist`). | `Sonic1GlassReflectionInstance` (fixed) | `TestSonic1GlassReflectionGraphRewind` (+1 new test, 1 existing test's formula-anchor assertion corrected) and `TestSonic1GlassBlockObjectInstance` GREEN after the fix. | `mvn "-Dtest=TestSonic1GlassReflectionGraphRewind" -Dsurefire.forkCount=1 test` (RED — delta=160px — against unmodified code; GREEN after the one-line anchor fix) | fixed |
| 5 | S1 object visuals | Lamppost head duplicate | General (any zone with a lamppost checkpoint object) | Reproduced with new `TestCheckpointStarpostGraphRewind#sonic1LamppostTwirlEndsAsOneCenteredBallNotADuplicate`: drives a real activation through `Sonic1LamppostObjectInstance.update()`, runs the twirl through completion, and asserts exactly one ball visual near the parent's X. Root cause: `docs/s1disasm/_incObj/79 Lamppost.asm` Lamp_Finish (routine 4, lines 122-123) is a plain `rts` — the pole's own mapping frame is never touched again this act (both pole and twirl child ARE still subject to the standard per-frame `out_of_range` delete via `RememberState`, `79 Lamppost.asm:6-11`; a prior version of this row's narrative and the code's own javadoc incorrectly claimed no such check exists at all — corrected post-merge, see below). `Sonic1LamppostObjectInstance.onTwirlComplete()` incorrectly forced the pole to `FRAME_RED` on the (false) theory that the engine destroys the twirl immediately; since the twirl is never destroyed by `Lamp_Finish`, this produced a second, exactly-centered ball stacked next to the twirl's own frozen resting ball. Fixed by removing the frame reassignment — `onTwirlComplete()` now only clears the `twirlActive` bookkeeping flag, matching ROM's no-op `Lamp_Finish`. **Post-merge follow-up (found during re-investigation of a related "resting spot sometimes wrong" report):** the twirl's own resting position was ALSO off by one frame — `Sonic1LamppostTwirlInstance.INITIAL_LIFETIME` was `0x21` (33) instead of ROM's `move.w #32,lamp_time(a1)` (`79 Lamppost.asm:105`, i.e. `0x20`), running one extra 22.5-degree orbit step and landing the ball at `(-4.6, -11.1)` from the twirl center instead of ROM's exact `(0, -12)`. Fixed by correcting the constant to `0x20`; the surrounding comment already documented the correct `0x20`/33-frame value, so only the field's numeric literal was wrong. The regression test's resting-position assertion was tightened from a loose `dx <= 12` (swing-radius) bound to the exact ROM-derived `(0, -12)` terminal offset, which is RED against `0x21` and GREEN against `0x20`. | `Sonic1LamppostObjectInstance`, `Sonic1LamppostTwirlInstance` (fixed) | `TestCheckpointStarpostGraphRewind` (+1 new test from the original fix; the same test's terminal-position assertion tightened to an exact ROM-derived value for the post-merge off-by-one fix) GREEN after both fixes. | `mvn "-Dtest=TestCheckpointStarpostGraphRewind" -Dsurefire.forkCount=1 test` (GREEN after fix) | fixed |
| 6 | S1 object visuals | Lavafall landing flicker | Marble Zone (MZ), lava geyser landing platform | Reproduced with two new `TestSonic1LavaGeyserOutOfRange` tests that drive a `Sonic1LavaGeyserMakerObjectInstance` through `GMake_MakeLava` inside a real `ObjectManager` exec loop. The simple same-slot-ordering case (`lavafallHeadNeverRendersAtLandingYOnItsSpawnFrame`) passed immediately (the object manager's `FindNextFreeObj`-equivalent usually schedules the new head into the same exec pass), so per the brief's "dig deeper before concluding not-reproducible" guidance, a second test placed the maker at the LAST dynamic slot (`lavafallHeadNeverRendersAtLandingYWhenNoSlotIsFreeAfterMaker`) to force `allocateSlotAfter` to fail and `spawnFreeChild`'s generic fallback to grab an already-passed slot — deferring the new head's own first `update()` to the next frame. That test was RED before the fix (head rendered at Y=1024, the maker's own landing Y) confirmed by temporarily reverting the production change. Root cause: `docs/s1disasm/_incObj/"4C, 4D MZ Lava Geyser and Maker.asm"` Geyser_Main (lines 157-172) falls straight through into Geyser_Action + DisplaySprite in the SAME pass GMake_MakeLava creates the object, so ROM never displays a lavafall head at its un-shifted landing Y — but the engine deferred the `-0x250` start-height shift to `ensureInitialized()` on the head's own first `update()`, which is only guaranteed same-frame when slot allocation happens to cooperate. Fixed by applying the shift eagerly in `Sonic1LavaGeyserObjectInstance`'s constructor (pure position arithmetic, no `services()` needed) instead of waiting for the first `update()`. | `Sonic1LavaGeyserObjectInstance` (fixed) | `TestSonic1LavaGeyserOutOfRange` (+2 new tests) and `TestSonic1LavaGeyserGraphRewind` (unchanged, still GREEN) after the fix. | `mvn "-Dtest=TestSonic1LavaGeyserOutOfRange,TestSonic1LavaGeyserGraphRewind" -Dsurefire.forkCount=1 test` (RED on the slot-exhaustion test before the fix, GREEN after) | fixed |
| 7 | Badnik and touch responses | Roller standing-state defeat | Spring Yard Zone (SYZ), Roller badnik spawn (any act) | `TestSonic1RollerBadnikInstance` (3 new tests) drives the private `secondaryState`/`invincible` fields to each of the Roller's three ROM lifecycle states and asserts `getCollisionFlags()`. Investigated against `docs/s1disasm/_incObj/43 Badnik - Roller.asm`: Roll_Main/Roll_Action_FromLeft (ob2ndRout=0, the initial curled/waiting state) never write `obColType` — it stays 0 (col_none) from the zeroed spawn RAM, so ReactToItem's `move.b obColType(a1),d0 / bne` skip (Sonic ReactToItem.asm:52-53) applies: a dormant, not-yet-activated Roller genuinely cannot be touched, hurt Sonic, OR be defeated. This is pre-existing, already-correct ROM-accurate behavior (landed in an earlier, unrelated fix citing S1 SYZ1 trace f2338 — commit `4e3f93780`, already on this branch before Task 4 started) and is NOT a code defect: once the Roller activates it is destroyable ($0E, ENEMY category) while stopped/unfolded and damaging-but-not-destroyable ($8E, HURT category) while rolling, both of which the current code already implements correctly and the new tests now lock in. No production change needed. | `Sonic1RollerBadnikInstance` (investigated, not changed — already ROM-accurate) | `TestSonic1RollerBadnikInstance` (+3 new tests) GREEN, confirming current behavior already matches ROM. | `mvn "-Dtest=TestSonic1RollerBadnikInstance" -Dsurefire.forkCount=1 test` (GREEN; no RED reproduction of a code defect found — the reported symptom describes the ROM's intentional "ambush enemy is untouchable until it activates" design) | rom-confirmed-intentional (no fix landed; ROM evidence disproves the "fix" framing — see Task 4 report) |
| 8 | Badnik and touch responses | Yadrin top spikes | Zone hosting the Yadrin badnik (Spring Yard Zone); exact act not yet confirmed this session | Reproduced with 2 new `TestSonic1YadrinBadnikInstance` tests driving a realistic ROM-accurate top-graze touch. Root cause, `docs/s1disasm/_incObj/Sonic ReactToItem.asm:532-561` (React_Yadrin): the spiked head is a real 24px-wide sub-hitbox offset 4px from Yadrin's leading edge (mirrored 16px further when facing right), gated on Sonic's react-hitbox bottom edge clipping <8px into Yadrin's top edge (a shallow graze, not a deep overlap). The prior implementation compared Sonic's and Yadrin's CENTRE Y values against that same <8 threshold; since a genuine top-graze touch has a centre-to-centre distance close to the sum of both hitbox half-heights (~30+px), that check almost never matched on first contact, so a rolling Sonic landing on the spikes fell through to the attacking-badnik branch and safely destroyed Yadrin instead of getting hurt — exactly the reported "spikes can be jumped on safely" symptom. It also never checked the spike's horizontal (X) window at all. Fixed `Sonic1YadrinBadnikInstance.isSpikeRegionHit()` to compute the ROM's edge-based vertical penetration (`sonicBottom - yadrinTop`, using `player.getYRadius() - 3` per ReactToItem.asm:20-21 and the real touch-hitbox `heightRadius` from `TouchResponseResult`) plus the mirrored 24px X-window check, matching React_Yadrin exactly. | `Sonic1YadrinBadnikInstance` (fixed) | `TestSonic1YadrinBadnikInstance` (2 existing tests recalibrated to ROM-accurate coordinates + 2 new tests) GREEN after the fix; RED before it (`attackingDeepOverlapDestroysBadnikNormally` failed its `assertFalse(isDestroyed)`/services-crash before the fix — see Task 4 report for the isolated RED capture). | `mvn "-Dtest=TestSonic1YadrinBadnikInstance" -Dsurefire.forkCount=1 test` (RED before fix, GREEN after) | fixed |
| 9 | Badnik and touch responses | Hurt-on-slope animation | General (any sloped terrain, any zone) | Investigated per Task 4's brief. Found a real, deliberate design decision at `PlayableSpriteMovement.modeAirborne()` (lines 892-898, cites S1 "01 Sonic.asm:1410" / S2 "s2.asm:37806"): the ground angle is intentionally NOT reset to zero while hurt, so a hurt knockback launched from a steep slope keeps its nonzero angle through the whole recoil. Added `TestPlayableSpriteMovement#hurtStatePersistsIdenticallyRegardlessOfGroundAngleAtImpact`, which drives `applyHurt()` from flat (0x00) and steep-slope (0x20) starting angles through several airborne-hurt `handleMovement()` ticks with a `LandingProbeCollisionSystem` that never reports ground contact (isolating the pure hurt/air/velocity state machine from real terrain sensor probing, which this test suite has no sloped `SolidTile`/`HeadlessTestRunner` level fixture to drive faithfully). Result: `hurt`, `air`, and the knockback velocities evolve identically regardless of starting angle — only the (correctly preserved) angle value itself differs, ruling out a defect in the pure hurt/air dispatch and mode-selection logic. This does NOT rule out the real terrain-collision sensor path detecting ground contact one frame early on steep terrain (a rotated/steep-ground-mode sensor snapping Sonic back to the slope's surface before a flat floor's sensor would) — reproducing that needs a real sloped-terrain level fixture this suite does not yet have. Per the brief's guidance, no speculative fix was landed. | Playable hurt logic (`PlayableSpriteMovement.modeAirborne()` / `AbstractPlayableSprite`) (investigated, not changed) | `TestPlayableSpriteMovement` (+1 new test) GREEN without any production change — the added coverage did not reproduce the symptom at the pure state-machine level. | `mvn "-Dtest=TestPlayableSpriteMovement" -Dsurefire.forkCount=1 test` (GREEN both before and after; no RED reproduction found) | blocked-needs-capture |
| 10 | Springs and bumpers | Hurt spring recovery | General (any zone with a spring) | Reproduced with 2 new `TestSonic1SpringObjectInstance` tests (`upSpringRestoresControlImmediatelyEvenWhileHurt`, `downSpringRestoresControlImmediatelyEvenWhileHurt`): set the player hurt+airborne, then fire a genuine spring `onSolidContact`/bottom-touch. Root cause: `docs/s1disasm/_incObj/41 Springs.asm:96` (`Spring_BounceUp`) and `:203` (`Spring_BounceDwn`) both execute `move.b #2,obRoutine(a1)` **unconditionally** — the spring pokes Sonic's own object routine straight to `Sonic_Control` (2) on every trigger, regardless of what routine he was in beforehand. Routine 4 is the hurt/knockback routine (`AbstractPlayableSprite.hurt`, doc'd as "Mirrors ROM routine=4 check"), so a spring firing on a hurt-and-airborne Sonic ends the hurt routine and restores control (D-pad + jump; gated on `sprite.isHurt()` in `PlayableSpriteMovement.handleMovement()` lines 472/481) the SAME frame the spring launches him — it does not wait for a normal grounded `Sonic_HurtStop`/`Sonic_ResetOnFloor` landing the way the engine's existing hurt-landing deferral (`setAirAfterObjectHurtLanding()`/`completeHurtLandingRecovery()`) models for ordinary platforms. Fixed by calling `player.setHurt(false)` at the same point in `applyUpSpring()`/`applyDownSpring()` where ROM writes `obRoutine(a1)=2`. Invulnerability frames are untouched (already granted at hit-time in `applyHurt()`, matching ROM) and now correctly resume ticking once hurt clears (`tickInvulnerabilityDisplayTimer()` freezes the timer while `hurt==true`). | `Sonic1SpringObjectInstance` (fixed) | `TestSonic1SpringObjectInstance` (+2 new tests, 4 total) GREEN after the fix; RED before it (both new tests failed `assertFalse(player.isHurt())`, expected `<false>` but was `<true>`). | `mvn "-Dtest=TestSonic1SpringObjectInstance" -Dsurefire.forkCount=1 test` (RED before fix — isolated via `git stash`; GREEN after) | fixed |
| 11 | Springs and bumpers | SYZ spring-bumper loop clipping | Spring Yard Zone (SYZ), spring-into-bumper loop section; exact act/coordinates still TBD (no automated coverage locates the specific in-ROM placement — see row 12/25 for the live-capture recipe) | Built a new headless fixture (`TestSonic1BumperObjectInstance#springBumperPinballLoopNeverGrowsPlayersEmbedDepthIntoSpringSolid`) driving BOTH objects' real `onSolidContact`/`onTouchResponse` methods through many simulated bounce cycles between a spring and a bumper 256px apart. **Found and fixed a real, distinct engine-only defect** in the bumper (not the spring): `Sonic1BumperObjectInstance.applyBounce()` computed the radial bounce direction from `player.getCentreX()/getCentreY()` read LIVE at consumption time inside `update()`, one frame after the touch was actually detected in `onTouchResponse()` (`ObjectManager.update()` runs every object's own `update()` before the touch-response pass, so a touch armed this frame is only consumed on that object's NEXT `update()` call — see `docs/s1disasm/_incObj/sub ReactToItem.asm:377-427` / `47 Bumper.asm:29-30`, where ROM's `Bump_Hit` reads Sonic's position in the SAME frame `React_Special` sets `obColProp`, with zero such delay). A fast-moving player (exactly the spring-launch scenario) can cross clean through the bumper's Y position during that single deferred frame; the debug-instrumented fixture proved this precisely: touch armed at frame 17 with the player still below the bumper (Y=779 vs bumper Y=768, approaching from below), consumed at frame 18 by which point `move()` had already carried the player to Y=767 — ABOVE the bumper — so the "bounce away" velocity came out pointing further UP instead of back DOWN, and the player never returned to the spring across 800 simulated frames (38 spurious upward bumper re-triggers, 1 spring trigger). This is fully consistent with, and a very plausible root cause of, the reported "SYZ spring-into-bumper can knock Sonic down repeatedly" symptom — a mistimed bounce direction from a fast-approaching player is exactly what would make the loop behave unpredictably instead of cycling cleanly. Fixed by capturing `player.getCentreX()/getCentreY()` in `onTouchResponse()` at the moment of the actual touch (new `pendingTouchCentreX`/`pendingTouchCentreY` fields) and using those captured coordinates in `applyBounce()` instead of re-sampling the live (now stale) position. Separately, the SPRING side of the loop was investigated and found already correct: `ObjectSolidContactController.resolveContactInternal`'s FULL_SOLID standing branch (lines 3871-3916, citing `s2.asm:35298`'s `cmpi.w #$10,d3 / blo Solid_Landed`) snaps the player's Y to the clean surface position BEFORE ever invoking the spring's `onSolidContact` listener, for any accepted landing depth in its ROM-matching [0,16) window; a tunnel-through deeper than that window is either rejected outright (pass-through, no embed) or resolved via the same controller's `SolidObject_LeftRight` minimum-penetration-axis side-eject (own position correction, not a static embed) — so `Sonic1SpringObjectInstance`'s own `+8` push-in (`applyUpSpring`/`applyDownSpring`) is bounded and self-correcting on every trigger regardless of entry depth; the fixture's own resting-position depth assertion (`maxObservedDepthPastRest == 8`) now passes and would fail if that ever regressed. | `Sonic1BumperObjectInstance` (fixed — touch-time position capture); `Sonic1SpringObjectInstance` (investigated, not changed — already ROM-correct); `ObjectSolidContactController` (investigated, not changed — out of this task's file scope, already ROM-cited) | `TestSonic1BumperObjectInstance` (new file, 2 tests) GREEN after the fix; the loop test was RED before it (isolated via `git stash` on just `Sonic1BumperObjectInstance.java` — "only 1 spring triggers (bumper touches: 38)"), GREEN after. | `mvn "-Dtest=TestSonic1BumperObjectInstance" -Dsurefire.forkCount=1 test` (RED before fix, GREEN after) | fixed |
| 12 | Springs and bumpers | Possible bumper/spring emulator parity | Cross-zone verification question (wherever springs feed directly into bumpers) | Per the plan's verification-only guidance: EmuHawk is available locally (`C:\Users\farre\Downloads\_Sorted\Emulators\BizHawk-2.11-win-x64\EmuHawk.exe`, wired into `tools\bizhawk\run_bizhawk_lua.bat`'s default fallback path) and S1 complete-run BK2 fixtures exist for all 3 SYZ acts (`src/test/resources/traces/s1/syz{1,2,3}_completerun`), but none of those routes is confirmed to traverse the specific spring-feeds-a-bumper section (no ROM object-placement coordinates for it were located this session), so a live capture pinpointing that exact section was not attempted here — doing so blind would require scanning the SYZ object-placement tables for a spring+bumper pair close enough to interact, then locating/recording a BK2 that actually visits it. Derived from disassembly instead (see row 11): ROM's `Bump_Hit` computes its bounce angle from the SAME-frame touch position (no cross-frame staleness is possible in ROM's slot-sequential `ExecuteObjects`), so the touch-timing defect fixed in row 11 is a pure engine-architecture artifact (deferred per-object `update()` consumption) that cannot occur on real hardware — ROM would never send Sonic the wrong direction off a bumper this way. Separately, ROM's generic `SolidObject`/`Solid_ChkCollision` dispatch (mirrored faithfully in `ObjectSolidContactController`, cross-checked against `s2.asm:35298` et al.) resolves an out-of-window vertical tunnel-through via its documented minimum-penetration-axis rule (side-eject), which is a well-known, ROM-faithful classic-Sonic physics quirk, not evidence of an engine-only embed bug. **Capture recipe for a human/BizHawk follow-up session:** (1) use `RomOffsetFinder --game s1 search "Bumper"` / inspect the SYZ object placement table to find a spring+bumper pair within ~1-2 screens of each other; (2) record or reuse a BK2 that visits that exact section (`tools\bizhawk\record_trace.bat` / `s1_trace_recorder.lua` for a fresh capture, or scan the existing `syz{1,2,3}_completerun` BK2s frame-by-frame with a small Lua probe watching `v_objspace` slot Y-velocity signs for the bumper+spring object IDs, `0x47`/`0x41`); (3) run a diagnostic Lua (pattern: `tools\bizhawk\diag_template_fast.lua`) logging Sonic's `y_pos`, `y_vel`, and the bumper/spring `obRoutine`/`obColProp` bytes for ~120 frames around the interaction; (4) compare the observed bounce-direction sequence against this engine's (now-fixed) behavior. This row's fix (row 11) already resolves the one engine-only defect the disassembly evidence points to; the live capture is only needed to confirm no OTHER ROM-side quirk exists in that exact section. | `Sonic1SpringObjectInstance`, `Sonic1BumperObjectInstance` | `TestSonic1BumperObjectInstance` (new, see row 11) now covers the object-level interaction; still no capture of the actual SYZ ROM section. | None (BizHawk capture not run — see recipe) | blocked-needs-capture (engine-side fix already landed per row 11; exact-ROM-section confirmation since closed by row 25/Task 11 - see row 25) |
| 13 | LZ water/current | Blocked doorway current softlock | Labyrinth Zone Act 1 (LZ1), the switch-3 door gating the water current (ROM object 0x56 subtype with `fb_type==3`) | Reproduced with 2 new `TestSonic1FloatingBlockObjectInstance` tests driving the real LZ1 switch-3 door object with a player positioned left of it. Root cause: `Sonic1FloatingBlockObjectInstance.handleLz1WaterTunnel()` was a stub (`"We skip this as the engine doesn't implement water tunnels yet"`), even though `Sonic1LZWaterEvents`'s wind-tunnel system is fully implemented and calls into `f_wtunnelallow`. ROM (`docs/s1disasm/_incObj/56 SYZ, SLZ Floating Blocks and LZ Doors.asm:219-243`, type05) gates the water current: while the LZ1 switch-3 door hasn't been triggered, it clears then re-disables `f_wtunnelallow` every frame based on whether the player has passed the door's X, and unconditionally re-enables it the same frame the switch is pressed (overriding the disable). Without this, the current pushes Sonic into the still-closed door indefinitely. Fixed by implementing `handleLz1WaterTunnel()`/`isLz1WaterTunnelDoor()` faithfully and adding the same re-enable call at the switch-press branch in `moveType05SwitchUp()`. | `Sonic1FloatingBlockObjectInstance` (fixed) | `TestSonic1FloatingBlockObjectInstance` (+2 new tests, 5 total) GREEN after the fix; RED before it (both new tests failed the `isWindTunnelDisabled()` assertions). `TestS1Lz1/2/3CompleteRunTraceReplay` still GREEN after the fix. | `mvn "-Dtest=com.openggf.game.sonic1.objects.TestSonic1FloatingBlockObjectInstance" -Dsurefire.forkCount=1 test` (RED before fix, GREEN after) | fixed |
| 14 | LZ water/current | Breath timer rewind | Any zone with water (S1/S2/S3K), underwater breath/countdown, general | Reproduced with new `TestAbstractPlayableSpriteRewindCapture#roundTripRestoresDrowningControllerState`: mutated `DrowningController`'s `remainingAir`/`frameTimer`/`drowningMusicStarted`/bubble-cadence fields, captured a rewind snapshot, mutated again, restored, and asserted the sentinels came back. Root cause: `AbstractPlayableSprite.captureRewindState()`/`restoreRewindState()` call `controller.getMovement()`, `controller.getSpindashDust()`, and `controller.getAnimation()` capture/restore, but never called `controller.getDrowning()`'s — `DrowningController` had no capture/restore methods at all, so a rewind seek left the breath timer, countdown-bubble cadence, and drowning-music audio-cue phase (`drowningMusicStarted`) completely untouched regardless of the rewound frame. Fixed by adding `DrowningController.RewindState`/`captureRewindState()`/`restoreRewindState()` and wiring them into `PlayerRewindExtra.drowningState` and both `AbstractPlayableSprite` capture/restore paths. This is shared cross-game code (S1/S2/S3K), verified with a cross-game trace sanity run. Same root cause as row 19 — dedupe: row 19 is now fixed by this same change. | `DrowningController`, `PerObjectRewindSnapshot`, `AbstractPlayableSprite` (fixed) | `TestAbstractPlayableSpriteRewindCapture` (+1 new test, 8 total) GREEN after the fix; RED before it (`remainingAir (breath timer) not restored` — expected 11, was 30). `TestRewindCoverageGuard`/`TestStaticStateRewindCoverageGuard` GREEN, no baseline change needed. Cross-game sanity: `TestS1Lz1CompleteRunTraceReplay` + `TestS2Ehz1TraceReplay` GREEN; `TestS3kAizTraceReplay` unchanged pre-existing failure (confirmed via `git stash` on unmodified tree, same frame/field). | `mvn "-Dtest=com.openggf.sprites.playable.TestAbstractPlayableSpriteRewindCapture" -Dsurefire.forkCount=1 test` (RED before fix, GREEN after) | fixed |
| 15 | LZ water/current | LZ2 1px gap between vertical waterfall column and slide water | Labyrinth Zone Act 2 (LZ2), reported vertical waterfall column vs. slide water seam | Genuinely probed by loading LZ1/LZ2/LZ3 and SBZ Act 3 (which reuses LZ water) through the real ROM object-placement pipeline (`ObjectManager.getAllSpawns()`, full/unwindowed) and scanning for `Sonic1ObjectIds.WATERFALL` (0x65/101) spawns. **Result: zero Waterfall object placements in any of LZ1, LZ2, LZ3, or SBZ3** in this ROM (`s1.gen`, verified REV01 CRC32/SHA-1 per CLAUDE.md) — the object exists in the disassembly (`docs/s1disasm/_incObj/65 LZ Waterfalls.asm`, `id_Waterfall` at object-pointer-table index 65) and its ROM code/mapping data (`_maps/Waterfalls.asm`, vertnarrow/vertwide column pieces) is real, but it is never placed in any water-zone act's static object layout table. The named object (`Sonic1WaterfallObjectInstance`) therefore has no code path that can exhibit a "1px gap" against the water surface in LZ2 (or any LZ act) — there is nothing to reproduce against. Separately verified `Sonic1WaterfallObjectInstance.ROUTINE_ON_WATER`'s `y = waterLevel - 0x10` and `Sonic1WaterSurfaceManager`'s `Y_OFFSET = -3`/mapping frames both match the disassembly (`WFall_OnWater`, `Map_Surf`/`Map_WFall_internal`) exactly, so no geometry defect was found in the code that IS reachable. **Blocked pending a precise capture**: the reported visual most likely comes from a different rendering path than the Waterfall sprite object (e.g. static BG/chunk tile art — LZ has its own waterfall palette cycle, `Pal_LZCyc1`/`Pal_SBZ3Cyc` in `PaletteCycle.asm` — or a different zone/act than LZ2). Recipe for follow-up: capture a screenshot or BizHawk frame number of the actual seam, identify whether the visible "column" is a sprite object or BG tile art at that position, and re-triage against the correct owner before any fix. | `Sonic1WaterfallObjectInstance` (investigated, not changed — object not reachable in LZ2) | No automated coverage added; a mapping/geometry assertion was not written because the object it would cover never spawns in the reported zone. | None (investigation only; see recipe above) | blocked-needs-capture |
| 16 | LZ water/current | Underwater conveyor spawn/despawn | Labyrinth Zone (LZ) / Scrap Brain Zone Act 3, LZ conveyor belt spawner (ROM object 0x63, `Sonic1ConveyorState` / `f_conveyrev` + `v_obj63`) | Reproduced with new `TestSonic1ConveyorStateRewindAdapter`. Root cause: `Sonic1ConveyorState` (ROM `f_conveyrev`/`v_obj63`) is a per-module static manager with mutable state + `reset()`, consumed across frames by `Sonic1LZConveyorObjectInstance` spawner/platform instances, but was never registered with the rewind registry — the same "trigger platform" bug class as `ButtonVineTriggerStaticAdapter` (S2) / `Sonic3kLevelTriggerStaticAdapter` (S3K) described in CLAUDE.md. Without coverage, a rewind seek to before a conveyor spawner's first visit left `v_obj63`'s dedup bit set; a rewind-recreated spawner re-checks `testAndSetSpawned()` in its own update and, seeing the stale "already spawned" bit, self-deletes via `ObjectLifetimeOps.deleteNoRespawn` without ever recreating its child platforms — the belt permanently disappears after a rewind past its spawn point. (The separate non-rewind "despawn incorrectly on camera re-entry" failure mode was already fixed previously — see `onUnload()`'s `makerDedupSlot` handling, pre-existing in this file — this task only found and fixed the rewind-specific gap.) Fixed by adding `Sonic1ConveyorState.Snapshot`/`captureRewindState()`/`restoreRewindState()` and a new `Sonic1ConveyorStateRewindAdapter`, registered via `Sonic1LevelEventManager.extraRewindAdapters()`. | `Sonic1ConveyorState`, `Sonic1ConveyorStateRewindAdapter` (new), `Sonic1LevelEventManager` (fixed) | `TestSonic1ConveyorStateRewindAdapter` (new, 3 tests) GREEN after the fix; RED before it (isolated by neutering the adapter's `restore()` — both round-trip and spawner-recreate assertions failed exactly as the bug predicts). `TestRewindCoverageGuard`/`TestStaticStateRewindCoverageGuard` GREEN, no baseline change needed. | `mvn "-Dtest=com.openggf.game.sonic1.TestSonic1ConveyorStateRewindAdapter" -Dsurefire.forkCount=1 test` (RED before fix via isolation, GREEN after) | fixed |
| 17 | Rewind and audio | Invincibility music cuts wrong | Cross-zone (any zone, invincibility monitor pickup) — held-rewind release while the override is still active | Reproduced with new `TestLiveRewindManagerAudioCleanup#releasingHeldRewindWhileAnOverrideIsStillActiveDoesNotEndItEarly`: sets up a real `HeadlessSmpsAudioBackend` with zone music playing and an invincibility-style override on top, drives a real held-rewind press+release through `LiveRewindManager`, and asserts the override survives release. Root cause: `LiveRewindManager.handleRealtimeRewindInput()`'s release cleanup called `cleanupPresentationAfterRealtimeRewind(AudioPresentationPolicy.STOP_TRANSIENT_SFX_RESYNC_MUSIC)` unconditionally on every held-rewind release. That policy's `AudioManager.afterRewindRestore()` handler calls `backend.stopAllSfx()` **and** `backend.restoreMusic()` — but the SAME cleanup path already calls `rewindController.commitDeferredAudioRestore()` immediately beforehand, which correctly rebuilds the backend's logical override stack/current-music state for the committed frame via `AudioKeyframeStore.replayToLogicalState()`. The unconditional `restoreMusic()` call queues `pendingRestore=true` regardless of whether that just-restored state says an override (invincibility/extra-life/Super Sonic — any music id in `GameAudioProfile.isMusicOverride()`) should still be active, so the very next real audio `update()` cycle (`doRestoreMusic()`) pops the override and cuts the music short — every time a player releases held rewind while still invincible, no matter how much duration remains. Fixed by adding a new `AudioPresentationPolicy.STOP_TRANSIENT_SFX` (stops transient SFX only, no forced music-stack pop) and switching the "release after a committed restore" call sites (`LiveRewindManager`'s normal release, `TraceSessionLauncher`'s equivalent) to it; `LiveRewindManager`'s `LEVEL_LOAD`/`SEAMLESS_LEVEL_TRANSITION` boundary handlers keep `STOP_TRANSIENT_SFX_RESYNC_MUSIC` (their deferred restore is intentionally *dropped*, not committed, since the new level's own init already re-established fresh audio — see `RewindController.dropDeferredAudioRestore()`'s doc comment — so those genuinely need the forced resync). Drowning-countdown music (row 19/(d)) is unaffected either way: `isMusicOverride()` does not include the drowning music id in any game profile, so it never lands on the override stack and `restoreMusic()` was already a no-op for it. | `AudioPresentationPolicy`, `AudioManager`, `LiveRewindManager`, `TraceSessionLauncher` (fixed — shared cross-game code) | `TestLiveRewindManagerAudioCleanup` (+1 new test, 5 total) and `TestAudioManagerRewindSuppression` (+1 new test) GREEN after the fix; RED before it (isolated by reverting just the `LiveRewindManager` release call site back to the old policy — `pendingRestore` expected `<false>` was `<true>`). | `mvn "-Dtest=TestLiveRewindManagerAudioCleanup,TestAudioManagerRewindSuppression" -Dsurefire.forkCount=1 -Dmse=off test` (RED before fix, GREEN after) | fixed |
| 18 | Rewind and audio | Speed shoes can last indefinitely | Cross-zone (any zone, speed shoes monitor pickup) — rewind restore after the timer would otherwise have already expired | Reproduced with new `TestLiveRewindSpeedModifiers#rewindRestoreKeepsTheSpeedShoesTimerExpiringAfterItsOriginalRemainingDuration`: gives a real `Sonic` sprite speed shoes, snapshots with 5 ticks remaining, drives the timer to natural expiry (confirming shoes correctly deactivate), then restores the snapshot and asserts the timer still expires after exactly its original remaining duration. Root cause: `TimerManager.capture()`/`restore()` (a game-agnostic registry with no knowledge of specific `Timer` subclasses) only preserve `(code, ticks)` pairs — `restore()` always rebuilds every entry as a behavior-inert `GenericTimer` whose `perform()` just self-removes, discarding the real `SpeedShoesTimer`'s `sprite` reference and its `perform()` callback (`sprite.deactivateSpeedShoes()` + restoring music tempo). After any rewind restore, the countdown still ticks down and vanishes on schedule, but `deactivateSpeedShoes()` is never called, so `speedShoes` stays `true` forever. Fixed at the smallest accurate owner — the sprite that already owns `speedShoes`/`giveSpeedShoes()`/`deactivateSpeedShoes()` (`TimerManager` itself is correctly generic and unaware of sprites; no change needed there). Added `PlayerRewindExtra.speedShoesRemainingTicks` (captured directly from the live `SpeedShoesTimer`'s remaining ticks, independent of `RewindRegistry`'s cross-subsystem restore ordering) and `AbstractPlayableSprite.restoreSpeedShoesTimer()`, which re-registers a real `SpeedShoesTimer` with that exact remaining duration (or removes the stale entry when `speedShoes` is false) during `restoreRewindState()`. | `AbstractPlayableSprite`, `PerObjectRewindSnapshot` (fixed); `TimerManager`, `SpeedShoesTimer` (investigated — correctly generic, not the owner) | `TestLiveRewindSpeedModifiers` (+1 new test, 7 total) GREEN after the fix; RED before it (`expected: <false> but was: <true>` on the final expiry assertion — isolated via `git stash` on `AbstractPlayableSprite.java`/`PerObjectRewindSnapshot.java`). | `mvn "-Dtest=com.openggf.game.rewind.TestLiveRewindSpeedModifiers" -Dsurefire.forkCount=1 -Dmse=off test` (RED before fix, GREEN after) | fixed |
| 19 | Rewind and audio | Breath countdown rewind | Underwater sections, general (likely LZ-specific in practice; see row 14) | Confirmed duplicate of row 14, fixed by the same change. See row 14 for the full root cause: `DrowningController` had no rewind capture/restore at all (breath timer, countdown-bubble cadence, and the drowning-music audio-cue phase `drowningMusicStarted` all silently dropped on rewind). Fixed by `DrowningController.RewindState` + `PlayerRewindExtra.drowningState` wiring in `AbstractPlayableSprite`. **Task 7 audio-cue verification (this task):** `DrowningController` triggers the countdown music via a plain `audioManager.playMusic(audioProfile.getDrowningMusicId())` call (`DrowningController.java:190,443`), which records into the same `AudioCommandTimeline`/`AudioKeyframeStore` machinery as every other music command — the same architecture verified sound while investigating row 17 (command-timeline replay correctly gates on `entry.frame() <= targetFrame`, so a rewind to before/after the countdown-start command replays or withholds it exactly as expected). No game profile (`Sonic1AudioProfile`/S2/S3K) includes the drowning music id in `isMusicOverride()`, so drowning music is never pushed onto the backend's override stack; row 17's `restoreMusic()`/`STOP_TRANSIENT_SFX_RESYNC_MUSIC` bug therefore could not have affected it either way (`restoreMusic()` is a no-op when the override stack is empty). The audio-cue phase of the breath countdown is covered by the same shared rewind-state audit as the timer/bubble state — no additional fix needed. | `DrowningController`, `PerObjectRewindSnapshot`, `AbstractPlayableSprite` (fixed — see row 14) | Same coverage as row 14: `TestAbstractPlayableSpriteRewindCapture#roundTripRestoresDrowningControllerState` (new) GREEN. | `mvn "-Dtest=com.openggf.sprites.playable.TestAbstractPlayableSpriteRewindCapture" -Dsurefire.forkCount=1 test` (GREEN after fix — see row 14 for RED evidence) | fixed |
| 20 | Rewind and audio | SLZ3 boss stops dropping balls | Scrap Brain Zone Act 3 (SLZ3) boss arena — after any rewind restore while the boss is alive | Reproduced with new `TestS1SlzBossSpikeballGraphRewind#restoreRebuildsTheBossSeesawScanCacheSoAlignmentCanStillMatch`: seeds the boss's lazily-scanned seesaw cache (as a real mid-fight `update()` would), captures/restores through the registry with in-place restore disabled (forcing recreate, which `Sonic1SLZBossInstance` always takes in practice — see below), then drives one real `update()` frame and asserts the cache repopulates. Root cause: `Sonic1SLZBossInstance.seesaws` (a `List<Sonic1SeesawObjectInstance>`, the boss's cached seesaw references, `_incObj` objoff_2A) is an object-reference list, which is not a supported default-capture field type (`ObjectManager.isRewindInPlaceReuseSafeClass()` requires every non-final field to be captured by the default reflective/compact-schema path; a `List<ObjectInstance>` is neither a scalar nor a `RewindStateful`-element "stateful list", so this class is never in-place-reuse-safe and a rewind restore always destroy/recreates it from its spawn). The companion `seesawsScanned` boolean flag (a scalar, captured fine) survives the restore as `true`, but the freshly-recreated instance's `seesaws` list starts empty — since `updateBossLogic()` only called `scanForSeesaws()` when `!seesawsScanned`, the stale `true` flag on an empty cache permanently blocked ever re-scanning, so `updateScanning()`'s alignment loop (`for seesaw in seesaws`) had nothing to iterate and the boss could never again transition to `STATE_BALL_SPAWN` — Robotnik keeps patrolling forever without dropping another spikeball after any rewind. Fixed by removing the separate `seesawsScanned` flag entirely and gating the lazy scan on `seesaws.isEmpty()` instead, making the cache self-healing regardless of how the object got restored (in-place or recreated) — a pure engine-side caching fix with no ROM-observable behavior change (the ROM caches its own seesaw addresses once at boss init; this ordering detail is an engine implementation choice, not a modeled ROM branch). | `Sonic1SLZBossInstance` (fixed) | `TestS1SlzBossSpikeballGraphRewind` (+1 new test, 7 total) GREEN after the fix; RED before it (`expected: <1> but was: <0>` on the post-restore-update() cache-repopulation assertion). | `mvn "-Dtest=com.openggf.game.rewind.TestS1SlzBossSpikeballGraphRewind" -Dsurefire.forkCount=1 -Dmse=off test` (RED before fix, GREEN after) | fixed |
| 21 | Diagnostics and logging | MZ getSpawnIndex identity miss | Marble Zone (MZ) Act 3, spawn at (4076,1696): logged `getSpawnIndex: identity miss for spawn at (4076,1696), found via equals at index 39` (plan Task 10) | `TestS1Mz3CompleteRunTraceReplay` with `-Dmse=off` does NOT reproduce it (pure forward trace replay never exercises a rewind/checkpoint restore). Root-caused instead by reading the call graph: `Sonic1RingInstance.recreateForRewind()` (the only rewind recreate hook that touches ring `RingSpawn` identity) built a fresh `new RingSpawn(x, y)` on every restore. **Coordinate/index match to the original report is now DIRECTLY CONFIRMED, not inferred:** a decisive probe loaded MZ3 through the real pipeline (`SharedLevel.load(SONIC_1, zone=1, act=2)`) and read `LevelManager.getCurrentLevel().getRings()` — a real `RingSpawn(4076, 1696)` exists in MZ3's ring list (exact match; the nearest `ObjectSpawn` in the layout table is the RING group's anchor at `(4052,1696) id=0x25 subtype=0x11`, 24px away — a sibling ring in the same group, not the same entry). Sorting that same 72-entry ring list by `AbstractPlacementManager`'s own `CHUNK_GRANULAR_ORDER` (chunk-aligned x) puts `(4076,1696)` at index 39 — an EXACT match to the reported "found via equals at index 39". This rules out `ObjectPlacementController` as the source of *this specific* report (no non-ring `ObjectSpawn` sits at that exact coordinate) and confirms `RingManager`/`Sonic1RingInstance` as the sole real owner. (`ObjectPlacementController`'s own identity-miss surface was spot-checked via three other rewind-recreate hooks named in review — `Sonic1CollapsingFloorObjectInstance`/`Sonic1CollapsingLedgeObjectInstance`/`Sonic1SpikedBallChainObjectInstance`'s fragment/chain-child `recreateForRewind` methods all reuse `ctx.spawn()` directly for dynamically-spawned children that were never registered in `ObjectPlacementController`'s layout-table spawn list in the first place, so they can't reproduce this identity-miss pattern; a full audit of every layout-table-object recreate hook was out of scope once the ring-list probe above conclusively identified this report's actual source.) Confirmed with a targeted unit test that builds a real `RingManager`, drives `recreateForRewind` through a `RewindRecreateContext`, and asserts (a) identity of the resulting `ringSpawn` field via reflection and (b) no `AbstractPlacementManager` "identity miss" log line when the restored instance is used afterward. | `Sonic1RingInstance.recreateForRewind` (root cause — fixed); `AbstractPlacementManager`/`ObjectPlacementController` (investigated, confirmed-correct for this report via the direct ROM-data probe above — the equals-based fallback is an intentional resilience mechanism, not the bug, and is exercised/pinned by the pre-existing `TestObjectPlacementManager#testMarkRememberedWithDifferentReferenceViaEqualsFallback`) | New `TestSonic1RingInstance#testRecreateForRewindResolvesCanonicalRingSpawnAvoidingIdentityMissWarning` (RED before fix: `assertSame` on the canonical `RingSpawn` failed with two structurally-equal-but-different instances; GREEN after adding `RingManager.resolveCanonicalSpawn(x, y)` and routing `recreateForRewind` through it). `TestRewindCoverageGuard`/`TestStaticStateRewindCoverageGuard` re-run clean (2/2, no baseline change needed). | `mvn "-Dtest=com.openggf.tests.TestSonic1RingInstance" "-DfailIfNoTests=false" -Dsurefire.forkCount=1 -Dmse=off test` (RED before fix, GREEN after; 12/12 total) | fixed |
| 22 | Diagnostics and logging | S1 SFX warnings | Cross-zone (any zone triggering the affected SFX during gameplay) | Two reproduction attempts, both against the pinned `s1.gen` (REV01) ROM: (1) a throwaway probe that constructed a real `Sonic1SmpsLoader` and called `loadSfx(id)` for every documented `Sonic1Sfx` enum value (0xA0-0xD0) plus the full neighboring range 0x90-0xE0 — expected at least one "Invalid SFX pointer"/"Invalid special SFX pointer" warning per the bug report; got zero warnings, every in-range id resolved to valid, non-null `Sonic1SfxData` (out-of-range ids returned `null` at `LOGGER.fine`, not `warning`, so they don't match the report either). (2) Ran the full 19-class S1 `*CompleteRunTraceReplay` suite with `-Dmse=off` (exercises real object interactions — rings, monitors, springs, badniks — across every S1 zone/act) — expected the reported warning to surface somewhere in 19 complete zone playthroughs; the only WARNING line across all 19 runs was the pre-existing, unrelated `Sonic1ObjectArt buildArtSheetFromRom` mapping-table warning. Also audited every `playSfx(...)` call site under `game/sonic1/` and every `GameSound`->id mapping in `Sonic1AudioProfile.SOUND_MAP`: all reference valid `Sonic1Sfx` enum ids (or an already-valid raw hex like `SFX_GIANT_RING=0xC3`), so no caller currently requests an out-of-table id through the base (non-donor) S1 loader. Separately noticed but NOT fixed (out of this bug's reproduced scope, no warning results from it today): `Sonic1SmpsLoader.loadSfx`'s range check `sfxId > Sonic1Sfx.ID_MAX` is non-strict at the shared boundary value 0xD0 (`ID_MAX` and `SPECIAL_SFX_ID_BASE` are both 0xD0 by design — WATERFALL is documented as needing the special pointer table), so id 0xD0 is dispatched through the *normal* SFX path instead of `loadSpecialSfx`; it happens not to warn only because `SFX_PTR_TABLE_ADDR + SFX_COUNT*4` is byte-identical to `SPECIAL_SFX_PTR_TABLE_ADDR` in this ROM, so the same valid pointer is read by coincidence (with a less-precise fallback blob-size calculation) — worth a follow-up ticket, but not the reported warning and not touched here per the "only modify production code if root-caused" rule. | `Sonic1SmpsLoader` (investigated, no reproducible defect found in `loadSfx` under the pinned ROM and current call sites) | No dedicated SFX-warning test exists or was added — nothing to pin, since the warning did not reproduce. | `mvn "-Dtest=com.openggf.tests.trace.s1.*CompleteRunTraceReplay" "-DfailIfNoTests=false" -Dsurefire.forkCount=1 -Dmse=off test` (19/19 GREEN, zero SFX/SmpsLoader warnings in the combined log) | blocked-needs-capture |
| 23 | Verification-only questions | SYZ ring slope behavior | Spring Yard Zone (SYZ), sloped ring-placement sections; exact coordinates TBD | **Task 11.** Root-caused directly from the ROM's own vertical-camera code — no live BizHawk session needed (feasibility fallback (ii): disassembly derivation, corroborated by the existing trace comparator). `docs/s1disasm/_inc/ScrollHoriz & ScrollVertical.asm:127-306` (`ScrollVertical`/`SV_ScrollSlow`/`SV_ScrollMedium`/`SV_ScrollFast`) caps the camera's vertical follow speed per frame — 2px inside the "at rest" sweet spot, 6px outside it at normal ground speed, only 16px once `obInertia` (ground speed) exceeds `$800` (asm:176-206) — regardless of how fast Sonic's own Y can change while rolling down a slope. `com.openggf.camera.Camera` (`FAST_SCROLL_INERTIA_THRESHOLD=0x800`, `DEFAULT_FAST_SCROLL_CAP=16`) implements the identical threshold/cap, cited to the same ROM lines. Rings are static world objects; when a rolling Sonic's vertical speed on a downhill slope outpaces the camera's capped follow speed, the camera legitimately falls behind him, so every static object ahead of him — including rings — correctly appears to "rise" toward the top of the screen relative to Sonic. This is the real Genesis game's documented vertical-scroll-lag mechanic, not a code defect and not ring-specific. | (verification-only; camera vertical-scroll code, already ROM-accurate) | `camera_y` is an actively-compared, pass/fail field in the trace replay comparator (`TraceBinder.compareNumeric("camera_y", ...)`, consumed by `AbstractTraceReplayTest`), and `TestS1Syz1/2/3CompleteRunTraceReplay` are full-route GREEN per `docs/TRACE_FRONTIER_LOG.md` — camera Y position matches ROM byte-for-byte through every SYZ act, including any downhill-roll sections that hit the scroll-speed cap. | None (disassembly derivation + existing trace-comparator evidence; no new capture needed) | rom-confirmed-intentional |
| 24 | Verification-only questions | SYZ running angle | Spring Yard Zone (SYZ); exact coordinates TBD | **Task 11.** Root-caused directly in the disassembly (feasibility fallback (ii)): `docs/s1disasm/_incObj/01 Sonic.asm:2223-2286` (`Sonic_Animate` `.walkrunroll`) selects the walking/running sprite-rotation octant from the raw `obAngle`, through a documented, `if FixBugs`-gated correction at asm:2229-2235 (`move.b obAngle(a0),d0` / `if FixBugs` / `ble.s .notoffbyone` / `subq.b #1,d0 ; adjust off-by-one angle on descending slopes` / `.notoffbyone:` / `endif`) — the disassembly's own comment names it "Fix off-by-one-radian error (this was implemented in S2/S3K)". `FixBugs` is a disassembly re-build convenience toggle, NOT part of the shipped, unpatched ROM pinned in CLAUDE.md (S1 World REV01, CRC32 `AFE05EEE`), so the genuine ROM never applies that subtraction: on a descending slope, S1 selects the running/walking sprite-rotation frame one angle-bucket later than S2/S3K, which both apply the identical `subq.b #1,d0` unconditionally, every frame (`s2.asm:38078-38080`; `sonic3k.asm:24816`/`29358` etc. — cited directly in the engine's own `Sonic2PlayerArt`/`Sonic3kPlayerArt`). This exactly explains "S1's running angle in SYZ looks one angle too much" relative to S2/S3K. | S1: `PlayableSpriteAnimation.resolveSlopeOffset()` / `ScriptedVelocityAnimationProfile.isAnglePreAdjust()` (investigated, confirmed already ROM-accurate) | Player physics `obAngle` is separately, actively compared every frame by the trace replay comparator (`sprite.getAngle()`, `AbstractTraceReplayTest`), and SYZ1/2/3 complete-run traces are GREEN — the underlying ground angle value is byte-accurate; the "one angle too much" perception is specifically the animation-frame-selection quantization, a value the trace comparator does not check (it never compares rendered `obFrame`/octant selection). Reading the engine source confirms it already reproduces the real ROM's unadjusted S1 selection: only `Sonic2PlayerArt`/`Sonic3kPlayerArt` call `.setAnglePreAdjust(true)`; no S1 profile ever does, and `TestAnimationTranslator#...` (`assertFalse(translated.isAnglePreAdjust())`) locks that in for the shared donation path. | None (disassembly derivation; angle-value trace comparator already GREEN; no code change needed) | rom-confirmed-intentional |
| 25 | Verification-only questions | Spring into bumper emulator parity | Cross-zone (wherever a spring feeds directly into a bumper, likely SYZ) | **Task 11.** Duplicate framing of row 12, now closed with direct trace evidence in place of a fresh BizHawk session (feasibility fallback (i): reuse the existing recorded S1 REV01 complete-run route, which "worked decisively for Task 9"). A ROM-object-placement scan (`ObjectManager.getAllSpawns()` over SYZ1/2/3, filtering `Sonic1ObjectIds.SPRING`(0x41)/`BUMPER`(0x47) within 400px of each other) located dense spring+bumper "pinball" clusters: SYZ1 x=5136-5360,y=1024-1474 (springs at y=1273/1474, bumpers at y=1024-1184) and SYZ3 x=1808-2032,y=1032-1370. Decoding `src/test/resources/traces/s1/syz1_completerun/physics.csv.gz` (hex-encoded columns) around the exact SYZ1 cluster (frames 0x102E-0x11DC, x=4700-5450, y=1091-1170 — the bumper-height band of the cluster) shows the real, recorded ROM route rolling straight through it with several genuine bumper-bounce impulses (e.g. frame 0x1108: y_speed 0x0000->0xF980/-1664 with status 0x04->0x16; frame 0x11D9: the same -1664 impulse recurs) and then continuing forward with normal progress — by construction (this is a complete, successful zone run), there is no unrecoverable repeated-knockdown loop trapping the recorded input at this ROM-verified bumper cluster. The recorded pass does not happen to trigger a spring immediately beforehand in this exact window, so a clean "spring-directly-into-bumper" combo was not isolated frame-for-frame in the existing recording, but the underlying bumper touch-response system is proven byte-for-byte correct against the real ROM at this exact, ROM-object-table-verified cluster. | `Sonic1BumperObjectInstance`, `Sonic1SpringObjectInstance` (fix already landed, row 11) | Same coverage as rows 11/12 (`TestSonic1BumperObjectInstance`), now supplemented with direct trace confirmation: `TestS1Syz1CompleteRunTraceReplay` is GREEN through the exact identified bumper cluster (byte-for-byte engine/ROM match, including the bounce impulses above), closing the "only the exact-ROM-section confirmation remains" gap row 12 left open. | None (existing recorded trace decoded; no new BizHawk capture needed — see feasibility fallback (i)) | fixed (bumper touch-timing defect resolved per row 11 and ROM parity confirmed byte-for-byte through the located cluster; the specific spring-directly-into-bumper combo itself remains unexercised in the recording - see caveat in evidence column) |
| 26 | Badnik and touch responses (Task 8 addendum) | SYZ3 boss leaves his spike in the air when he retreats | Spring Yard Zone Act 3 (SYZ3) boss arena, post-defeat retreat (DEFEAT_WAIT/ASCENT/ESCAPE) | A real, adjacent, ROM-cited divergence was found and fixed (commit `eacd3ac96`): `Sonic1SYZBossInstance` had the boss *push* the spike's collision/extension state into `SYZBossSpike` from `updateBossLogic()` via `updateSpikeState()` — but `updateBossLogic()` is skipped for exactly one frame during the post-hit defeat dispatch deferral (`defeatDeferralAppliesToThisBoss()`, needed for correct camera-boundary timing). ROM's `BossSpringYard_SpikeMain` (docs/s1disasm/_incObj/75, 76 Boss - SYZ Main and Blocks.asm) is a fully independent SST object slot that reads the boss's `ob2ndRout`/`obSubtype`/`GenericTimer` fresh every frame via its own dispatch, so the push model left the spike's collision one frame stale right at the killing hit. New `TestSonic1SyzBossInstance` (2 tests) reproduces and locks this in: RED before the fix on the same-frame-harmless assertion, GREEN after. **However, the reported symptom itself was NOT reproduced by this fix or by any test written for it.** The killing-hit staleness is a single-frame collision-only bug at the *start* of DEFEAT_WAIT — roughly 180+42 frames *before* the boss actually flies away in ESCAPE — and cannot explain a visibly "hanging" spike during the retreat flight. Code-review verification (confirmed by re-reading `SYZBossSpike.getX()`/`getY()` — live pass-throughs to `parent.getX()`/`getY()` every render call — and `AbstractBossChild.isDestroyed()` — lazily re-checks `parent.isDestroyed()` on every call) shows spike position and destroy-timing were *already* architecturally guaranteed to track the boss with no lag, independent of this fix; the second new test (`spikeIsRemovedTheSameFrameTheBossSelfDestroysOffScreenDuringEscape`) passes unmodified even against the pre-fix code, confirming this. The actual "hangs in the air" mechanism remains unexplained. **Capture recipe for re-triage:** (1) BizHawk or in-engine video/frame-log capture of a full SYZ3 boss fight through defeat and the ESCAPE flight, with the spike sprite visible on screen; (2) log/inspect the boss's `ob2ndRout` (routineSecondary) transitions frame-by-frame around DEFEAT_WAIT->ASCENT->ESCAPE, plus the spike's own slot/`spikeActive`/`extensionDepth`/`isDestroyed()` fields each frame, specifically watching the ASCENT->ESCAPE boundary and the frames where the boss visually leaves the screen; (3) **candidate worth checking first:** whether a REWIND performed mid-fight (before or during the retreat) leaves a restored `SYZBossSpike` with a stale/wrong parent link or duplicate instance — `SYZBossSpike.recreateForRewind()` independently relinks via `RewindRecreateObjectLinks.nearestObject(ctx, Sonic1SYZBossInstance.class, true)` while the boss's own constructor also unconditionally re-spawns a spike via `spawnSpikeChild()`, and whether these two paths can ever produce two live spikes (one correctly tracking the boss, one orphaned and frozen) after a rewind restore during the fight has not been tested — no fix should be attempted here without an isolated repro first. | `Sonic1SYZBossInstance`, `SYZBossSpike` (adjacent bug fixed; reported symptom still open) | `TestSonic1SyzBossInstance` (new file, 2 tests) GREEN after the fix; RED before it on the killing-hit test only (`expected: <false> but was: <true>` on the same-frame harmless assertion — isolated via `git stash` on just the two boss/spike files). The escape-destroy-sync test was GREEN even pre-fix, i.e. it is a lock-in guard, not a reproduction, of the reported symptom. | `mvn "-Dtest=TestS1SyzBossBlockGraphRewind,TestSonic1SyzBossInstance,TestS1Syz3CompleteRunTraceReplay" "-DfailIfNoTests=false" -Dsurefire.forkCount=1 -Dmse=off test` (RED before fix on the new test, GREEN after; 8/8 total, no trace regression) | blocked-needs-capture (adjacent one-frame collision-staleness bug fixed; the reported "spike hangs in the air during retreat" symptom is unreproduced and needs a capture per the recipe above before further work) |
| 27 | Object collision (Task 9) | SLZ2 staircase has a single blocking pixel — Sonic running across it gets stopped by a 1px obstruction that shouldn't block him | Sonic 1 Scrap Brain Zone Act 2 (SLZ2), reported coordinate X5611/Y58 (approximate/likely top-left); located the real ROM placement via `ObjectManager.getAllSpawns()`: Object 0x5B (Staircase) placement index 104, x=5648 y=112, subtype=0x00, flags=0x01 (xFlip) | Reproduced headlessly with a real `HeadlessTestFixture` against the actual SLZ2 level (`SharedLevel.load(SONIC_1, zone=4, act=1)`): approaching the staircase's leftmost piece from the left at ground level and holding only right traps Sonic in a bounded x oscillation (never advances past the piece's left collision edge, `pieceX(0)-halfWidth = 5648-27 = 5621`) — engine `ObjectSolidContactController.resolveContactInternal`'s ROM-cited "SolidObject_SideAir barely-poking" `absDistY<=4` vs `>4` boundary (citing `docs/s1disasm/_incObj/sub SolidObject.asm:181-184,211-214`) flip-flops frame to frame because the ground's own natural 1px height quantization on approach sits exactly on that threshold. **Cross-checked against the real ROM S1 REV01 complete-run trace for SLZ2** (`src/test/resources/traces/s1/slz2_completerun/physics.csv.gz`): the SAME trap appears in the recorded ROM run, frames 0x05D4-0x063E (~106 frames, input=0x0008 right-only), x oscillating in [5621,5624], y toggling 77/78/82 — byte-identical shape to the engine reproduction — and the real route only escapes by adding a jump at frame 0x063F (input=0x0018, y_speed goes negative, air=1). **Verdict: rom-confirmed-intentional.** This is a genuine ROM design quirk (the piece's left edge sits close enough above the approach ground that only a jump clears it; walking alone traps in place in both ROM and the engine) — not an engine defect. No production code was changed. | `ObjectSolidContactController` (investigated only, confirmed already ROM-accurate; no change) | New `TestSonic1Slz2StaircaseLeftEdgeThreshold` (2 tests) GREEN: locks in the ROM-matching bounded-oscillation trap when holding only right, and confirms a jump clears the same edge (matching the trace's frame-0x063F escape). `TestSonic1StaircaseWallCollision`/`TestSonic1StaircaseActivation`/`TestS1Slz2CompleteRunTraceReplay` unchanged and GREEN. | `mvn "-Dtest=TestSonic1StaircaseWallCollision,TestSonic1StaircaseActivation,TestS1Slz2CompleteRunTraceReplay,TestSonic1Slz2StaircaseLeftEdgeThreshold" "-DfailIfNoTests=false" -Dsurefire.forkCount=1 -Dmse=off test` (GREEN both before and after — no engine change was needed; the new tests characterize existing, already-ROM-accurate behavior) | rom-confirmed-intentional |

## Notes for the next session

- Rows 14/19 (breath timer/countdown rewind) were confirmed as the same underlying bug and
  fixed together by Task 6 (`DrowningController` rewind capture/restore, see row 14). Rows
  12/25 (spring/bumper emulator parity) remain a separate open duplicate pair — still
  `blocked-needs-capture` per row 12.
- Rows with "no known automated coverage exists" (3, 5, 7, 9, 16, 17, 22) need either a new
  focused unit test or a BizHawk capture before any fix in that area can be verified against a
  red baseline, per the plan's Global Rule "reproduce the bug or produce a targeted diagnostic
  ... before changing behavior." Row 16 was resolved by Task 6 with new coverage
  (`TestSonic1ConveyorStateRewindAdapter`). Row 15 was genuinely probed by Task 6 (real ROM
  object-placement scan) and found `blocked-needs-capture` for a different reason: the named
  object never spawns in the reported zone at all — see row 15 for the precise recipe.
- Full baseline logs are saved in this worktree's `target/surefire-reports/` from this session's
  Step 2 (retry with `-Dsurefire.forkCount=1`) and Step 3 runs.
- Rows 17/18/20 (rewind-audio/rewind-boss workstream) were closed by Task 7: a shared
  `LiveRewindManager`/`AudioPresentationPolicy` fix (row 17), a smallest-accurate-owner
  `AbstractPlayableSprite` fix (row 18), and an engine-side `Sonic1SLZBossInstance` caching fix
  (row 20). Row 19 was re-verified per Task 7's brief and remains resolved by Task 6 — no
  additional fix needed; drowning music is never a stack-based override so it could not have
  hit row 17's bug. Note when re-running `mvn -Dtest=...` for these rows: this worktree's MSE
  (`-Dmse=relaxed`, the repo default) intermixes an unrelated ambient full-suite failure summary
  even when `-Dtest` scopes correctly — pass `-Dmse=off` for a trustworthy per-class result if a
  `-Dtest`-scoped run appears to show failures in classes you did not request.
- Task 11 closed all three verification-only rows (23/24/25) via disassembly derivation and
  existing recorded-trace decoding — no live BizHawk session was needed for any of them (see
  each row for the exact evidence). Row 25's trace-based confirmation also closes the
  "only the exact-ROM-section confirmation remains" gap row 12 explicitly left open: row 12's
  own status line is now superseded by row 25's — the located SYZ1 spring+bumper cluster
  (x=5136-5360,y=1024-1474) has a byte-for-byte GREEN `TestS1Syz1CompleteRunTraceReplay` pass
  straight through it.
- **Tracked follow-up, not yet fixed (row 5, S1 lamppost twirl):** `Sonic1LamppostTwirlInstance
  .recreateForRewind()` / `findNearestLiveParentForRewind()` relinks a rewind-restored twirl to
  whichever live `Sonic1LamppostObjectInstance` is geometrically NEAREST at restore time, not
  necessarily the lamppost that actually spawned it. Repro sketch: two lampposts close enough to
  both stay loaded at once (e.g. the existing `S1_FAR_LAMP`/`S1_NEAR_LAMP` test fixtures, 0xC0
  apart); activate the near one so it spawns a twirl; capture a rewind snapshot mid-orbit; tear
  down the true (near) parent without also tearing down the twirl (off-screen unload /
  act-transition object-table rebuild / editor teardown race); restore the snapshot with only the
  far lamppost still live — the twirl silently reattaches to the far post even though its own
  captured `currentX`/`currentY` still reflect the near post's location. This needs a design call
  before fixing, not a one-line patch: either capture the parent's identity (via the existing
  `RewindIdentityTable`/`ObjectRefId` machinery) and drop the twirl when that exact parent isn't
  live at restore time (matching `missingS1LamppostDropsTwirlCleanly`'s "always exactly correct or
  absent" behavior), or add a distance/threshold guard so a "nearest" match farther than the
  twirl's own `SWING_RADIUS`-scale distance from its captured position is treated as "no match."
- **Post-merge finding, now fixed (row 6, S1 lavafall flicker):** row 6's original fix (applying
  the lavafall's `-0x250` start-height shift eagerly in `Sonic1LavaGeyserObjectInstance`'s
  constructor) closed the "lands at the wrong Y for one frame" half of the symptom, but a
  follow-up investigation (`.superpowers/sdd/geyser-anim-report.md`) found a second, distinct gap
  in the *maker* itself: `Sonic1LavaGeyserMakerObjectInstance.updateMakeLava()` (routine 6) set
  `currentAnim`/`animFrameIndex`/`animTimer` for the new eruption but never recomputed
  `displayFrame` in the same pass, so a repeating lavafall/geyser maker rendered one leftover
  frame of the *previous* cycle's `.bubble3` "ending" animation at the instant the next eruption
  became visible — matching the reported "ending animation shown at the start" symptom exactly
  (ROM's `GMake_MakeLava` falls through into `GMake_Display`'s own `AnimateSprite` call in the
  SAME dispatch pass, docs/s1disasm/_incObj/"4C, 4D MZ Lava Geyser and Maker.asm":64-87). Fixed by
  calling `updateAnimation()` once after setting the new anim state in both branches of
  `updateMakeLava()`. New RED-before/GREEN-after coverage:
  `TestSonic1LavaGeyserOutOfRange#lavafallMakerNeverRendersPreviousCycleEndingFrameAtNextEruption`.
- **Post-merge finding, now fixed (row 7, S1 Roller standing-state defeat):** row 7's collision
  investigation was correct and remains unchanged — a dormant, not-yet-activated Roller
  (`ob2ndRout=0`) genuinely has `obColType=0` and cannot be touched, hurt Sonic, or be defeated.
  A follow-up investigation (`.superpowers/sdd/roller-activation-report.md`) found that the
  engine additionally rendered and idle-animated the curled pose for the object's entire dormant
  lifetime, while ROM never calls `DisplaySprite`/`AnimateSprite` for it at all in that window —
  `Roll_Main` (routine 0) is entered via `jmp` and its own `rts` never reaches either call, and
  `Roll_Action_FromLeft` (`ob2ndRout=0`) pops its own return address (`addq.l #4,sp`) on every
  call specifically to skip them (docs/s1disasm/_incObj/"43 Badnik - Roller.asm":36-39,99). So the
  player was seeing a static, hitbox-less sprite ROM never draws — not just an untouchable one.
  Fixed by gating `Sonic1RollerBadnikInstance.appendRenderCommands()` and `updateAnimation()` on
  `!initialized || secondaryState == STATE_ROLL_CHK`, matching ROM's skip exactly without
  affecting the activated (rolling/unfolded) render or animation paths. New RED-before/GREEN-after
  coverage: `TestSonic1RollerBadnikInstance#dormantRollerEmitsNoRenderCommands` and
  `#dormantRollerDoesNotAnimate` (plus `#activatedRollerStillRenders` locking in the unaffected
  active-state path).
- **Post-merge finding, disposition unchanged (row 4, glass reflection oscillation):** a third
  investigation round (`.superpowers/sdd/glass-oscillator-report.md`) verified the exact byte/
  target/amplitude of the reflection's oscillator term at the opcode level
  (`sonic.lst:42561-42592`, `Glass_Type04`'s bit-3-set path): entry index 4 of the shared
  oscillator table (`v_oscillate+$12`, speed=4, limit=$20, start value $0080/direction "up" —
  matching `OscillationManager`'s `S1_SPEEDS[4]`/`S1_LIMITS[4]`/`S1_INITIAL_VALUES[4]`/
  `S1_INITIAL_DELTAS[4]` exactly) sweeps a continuous **62px peak-to-peak** vertical range
  (46px up / 16px down from baseline) on a ~252-frame (~4.2s) cycle, unconditionally, regardless
  of the parent block's trigger/`glass_dist` state — confirming the row's earlier
  `rom-confirmed-intentional` disposition rather than overturning it, but correcting the earlier
  round's undercounted amplitude guess (`±0x10..+0x30`, ~48px) with the ROM-computed real range
  (−16..+46, 62px). This was independently cross-checked by a fourth round
  (`.superpowers/sdd/glass-visual-diff-report.md`) via BOTH a BizHawk hardware RAM+screenshot
  capture and the engine's own headless `TraceCaptureTool` pixel diff of the same MZ2 block,
  both confirming the engine reproduces real hardware frame-for-frame. No code fix landed — this
  is deliberate ROM behavior, not an engine defect. Added a pinning test,
  `TestSonic1GlassReflectionGraphRewind#type4ReflectionSwings62PxWhileParentStationary`, driving
  a real Type04 (switch-activated, subtype `0x14`) block + its spawned reflection child through
  260 real `ObjectManager` update cycles with the switch never pressed, asserting the reflection's
  Y sweeps exactly `[baseline-46, baseline+16]` (both extremes reached, never exceeded) so a
  future "fix" attempting to address the original user report cannot silently dampen or remove
  this ROM-verified motion without a fresh disasm citation overriding this evidence.
